rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for testing
    // IMPORTANT: Change these rules before going to production
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Production-ready rules (commented out for testing)
    /*
    // Teams collection
    match /teams/{teamId} {
      allow read, write: if request.auth != null;
    }
    
    // Team members can read/write their team data
    match /teams/{teamId}/members/{memberId} {
      allow read, write: if request.auth != null && request.auth.uid == memberId;
    }
    
    // Availability collection
    match /availability/{availabilityId} {
      allow read, write: if request.auth != null;
    }
    
    // Test collection for connection testing
    match /test/{testId} {
      allow read, write: if true;
    }
    */
  }
}