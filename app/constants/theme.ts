/**
 * DEPRECATED: Use ../theme instead
 * This file is kept for backwards compatibility only
 * All new components should import from '../theme'
 */

// Re-export from new CS2 theme system for backwards compatibility
export { 
  Colors,
  Typography,
  Spacing,
  BorderRadius,
  Shadows
} from '../theme';